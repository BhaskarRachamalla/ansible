- name: variables from inventory
  hosts: localhost  

  tasks:
    - name: print variables from inventory
      ansible.builtin.debug:
       msg: "am learing {{}} from {{}} and total duration is {{}}"
- name: variables
  hosts: localhost  
  vars:
    courses: "ansible"
    trainer: "siva"
    duration: "12hrs"
  tasks:
  - name: print info
    var: 
      courses: "devops"
    ansible.builtin.debug:
     msg: "am learing {{courses}} from {{trainer}} in {{duration}}"
  - name: print info
    ansible.builtin.debug:
     msg: "am learing {{courses}} from {{trainer}} in {{duration}}"   
